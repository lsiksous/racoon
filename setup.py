from setuptools import find_packages, setup

setup(
    name='racoon',
    packages=find_packages(include=['racoon']),
    version='0.1.0',
    description='Set of python helpers',
    author='Laurent Siksous',
    license='MIT',
    install_requires=[],
)
